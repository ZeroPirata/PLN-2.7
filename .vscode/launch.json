{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "PLN: Fatec",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["src.main:app", "--reload", "--host", "127.0.0.1", "--port", "8000"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "pln-fatec"
    }
  ]
}
